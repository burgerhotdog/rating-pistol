import { Box, Typography } from "@mui/material";

const TeamTable = ({ gameId, userId, avatarCache, teamCache, setTeamCache, sortedAvatars }) => {
  return (
    <Box>
      <Typography variant="h6">
        Teams
      </Typography>
    </Box>
  );
};

export default TeamTable;
